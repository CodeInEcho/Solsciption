/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['nftstorage.link', 'cdn.helius-rpc.com'],
    },
}

module.exports = nextConfig
